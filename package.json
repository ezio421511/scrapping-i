{
  "name": "scrapping-i",
  "version": "1.0.0",
  "description": "",
  "main": "background.js",
  "scripts": {
    "build": "vite build",
    "build:background": "vite build --config vite.background.config.js",
    "build:content": "vite build --config vite.content.config.js",
    "build:popup": "vite build --config vite.popup.config.js",
    "build:all": "npm run build:background && npm run build:content && npm run build:popup"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "vite": "^7.1.7"
  }
}
